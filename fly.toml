app = "kompli-bot"  # оставь имя приложения как у тебя на Fly
primary_region = "sin"

kill_signal = "SIGINT"
kill_timeout = "5s"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

[processes]
  app = "python main.py"
